using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace Rapido.Application.Infrastructure.Validation.Validators
{
    /*
    public class CheckedValidator
    {

        public void ValidateAcceptanceOf(string key, bool? valueToAccept, string errorMessage = null)
        {

            if (valueToAccept.HasValue)
            {
                //if (!Validator.IsChecked(valueToAccept) == true)
                //    SetErrorMessageForKey(key, key + ": acceptance required", errorMessage);
            }
        }
        
    }
     */
}